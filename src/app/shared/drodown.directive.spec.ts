import { DrodownDirective } from './drodown.directive';

describe('DrodownDirective', () => {
  it('should create an instance', () => {
    const directive = new DrodownDirective();
    expect(directive).toBeTruthy();
  });
});
